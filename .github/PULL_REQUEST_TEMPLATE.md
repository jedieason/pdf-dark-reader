## What changed?

Describe the user-visible problem and resulting behavior.

## Validation

- [ ] `npm test` passes.
- [ ] I ran `npm run build` if I changed rendering, packaging, or generated extension files.
- [ ] I tested a normal text PDF and an image-heavy PDF if rendering changed.
- [ ] I updated documentation and screenshots if user-visible behavior changed.

Add before/after screenshots for visual changes, or explain why they are not needed.
